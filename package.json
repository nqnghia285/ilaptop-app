{
   "name": "ilaptop-app",
   "version": "1.0.0",
   "repository": "https://github.com/nqnghia285/ilaptop-app.git",
   "author": "nghia <nqnghia285@gmail.com>",
   "license": "MIT",
   "scripts": {
      "heroku-prebuild": "cd front-end && yarn install && cd ../back-end && yarn install",
      "deploy": "sh ./deploy.sh",
      "prebuild:be": "cd back-end && yarn migrate:deploy && yarn generate && yarn copy:.prisma:to:fe",
      "build:fe": "yarn build:fe:copy",
      "build": "yarn prebuild:be && yarn build:fe && yarn build",
      "start": "cd back-end && yarn start",
      "git": "sh git-push.sh"
   },
   "engines": {
      "node": "16.x",
      "yarn": "1.x"
   }
}
