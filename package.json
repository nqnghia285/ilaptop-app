{
   "name": "ilaptop-app",
   "version": "1.0.0",
   "repository": "https://github.com/nqnghia285/ilaptop-app.git",
   "author": "nghia <nqnghia285@gmail.com>",
   "license": "MIT",
   "scripts": {
      "heroku-prebuild": "cd front-end && yarn install --prod=false && cd ../back-end && yarn install --prod=false",
      "deploy": "sh ./deploy.sh",
      "prebuild:be": "cd back-end && yarn migrate:deploy && yarn generate && yarn copy:.prisma:to:fe",
      "build": "yarn prebuild:be && yarn build:fe:copy && yarn build",
      "start": "cd back-end && yarn start",
      "git": "sh git-push.sh"
   },
   "engines": {
      "node": "16.x",
      "yarn": "1.x"
   }
}
